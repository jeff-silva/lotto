# Migrator Addon

1.  Put this files in `/app/Addon/Router`.
1.  Register `RouterServiceProvider` in `/bootstrap/providers.php`.
1.  Give a look how Router works in `Examples` folder.
1.  Create controllers extends to `RouterController`.
1.  All done. Routes and OpenAPI Swagger Documentation will be created automatically.
