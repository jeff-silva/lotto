# Migrator Addon

1.  Put this files in `/app/Addon/Migrator`.
1.  Register `MigratorServiceProvider` in `/bootstrap/providers.php`.
1.  Give a look how Migrator works in `Examples` folder.
1.  Create your Migrators. All files need to have `Migrator` suffix.
1.  Run `php artisan migrator:run`.
